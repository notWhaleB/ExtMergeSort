#ifndef HSE_EM_CONSTANTS_H
#define HSE_EM_CONSTANTS_H

#include <cstdint>

typedef uint64_t int_t;

const size_t KiB = 1024;
const size_t KB = 1000;
const size_t MiB = 1024 * KiB;
const size_t MB = 1000 * KB;
const size_t GiB = 1024 * MiB;
const size_t GB = 1000 * MB;

#endif //HSE_EM_CONSTANTS_H
